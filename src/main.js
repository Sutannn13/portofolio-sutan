/* ============================================================
   MAIN JS ENTRY — Vite
   ============================================================ */

// Import CSS (Vite handles PostCSS/Tailwind processing)
import './main.css'

// Import enhanced modules (Three.js, SplitType, etc.)
import './enhanced-modules.js'

// The main script.js is loaded via <script> tag in HTML
// because it depends on GSAP/Lenis from CDN being available first

